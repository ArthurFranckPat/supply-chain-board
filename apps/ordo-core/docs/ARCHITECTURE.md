# Architecture — Ordonnancement Production v2

> Auto-generated by learn workflow. Last updated: 2026-04-08.

---

## 1. Overview

Ordo v2 is a manufacturing production scheduling system for an industrial site producing BDH (Boues De forage / Huiles) products. It handles:

- **Order management**: MTS (Make To Stock), MTO (Make To Order - ALDES), NOR (Normal)
- **Component feasibility**: Recursive BOM checking down to purchased components
- **Production scheduling**: Day-by-day, line-by-line planning with capacity constraints
- **Decision support**: Static rule engine + optional LLM (Mistral) for complex decisions
- **Reporting**: Action reports, service-rate KPIs, kanban risk analysis, charge heatmaps

---

## 2. Module Map

```
ordo-v2/
├── main.py                 # Thin entry point → src.main.main()
├── menu.py                 # Interactive Rich REPL (questionary)
├── analyze_charge.py       # Standalone charge analysis CLI
├── run_llm_commandes_v2.py # LLM-assisted order analysis script
├── patch_engine.py         # Dev tool: capacity smoothing patch
├── patch_smooth.py         # Dev tool: 2-line → N-line generalization patch
├── config/
│   └── weights.json        # Scheduler weight configuration
├── data/
│   ├── statique/           # Reference data (articles, gammes, nomenclatures)
│   └── dynamique/          # Operational data (besoins, OFs, stock, receptions)
├── src/
│   ├── main.py             # Core CLI entry (argparse)
│   ├── main_s1.py          # S+1 pipeline implementation
│   ├── models/             # Domain dataclasses
│   ├── loaders/            # CSV loading + DataLoader query interface
│   ├── algorithms/         # Core business algorithms
│   ├── checkers/           # Feasibility verification (immediate/projected/recursive)
│   ├── scheduler/          # Production scheduling engine
│   ├── agents/             # Decision engine (static rules + LLM)
│   ├── reports/            # Report generation (action, S+1)
│   ├── api/                # REST API (FastAPI)
│   ├── app/                # Application service layer
│   └── utils/              # Formatting utilities
├── tests/                  # Test suite
└── docs/                   # Documentation
```

---

## 3. Data Models (`src/models/`)

### 3.1 Article (`article.py`)

```python
@dataclass
class Article:
    code: str                    # PK
    description: str
    categorie: str               # AP, APV, PF3, PFAS, AFANT, ST*, etc.
    type_appro: str              # "ACHAT" | "FABRICATION"
    delai_reappro: Optional[int] # Days
```

Methods: `is_achat()`, `is_fabrication()`, `is_fantome()` (category == "AFANT")

### 3.2 BesoinClient (`besoin_client.py`)

```python
@dataclass
class BesoinClient:
    nom_client: str
    code_pays: str               # "FR" for France, other for export
    type_commande: TypeCommande  # "MTS" | "NOR" | "MTO"
    num_commande: str
    nature_besoin: NatureBesoin  # "COMMANDE" | "PREVISION"
    article: str                 # FK → Article
    of_contremarque: str         # MTS only: linked OF number
    date_commande: Optional[date]
    date_expedition_demandee: date
    qte_commandee: int
    qte_allouee: int
    qte_restante: int
```

Methods: `is_mts()`, `is_nor_mto()`, `est_commande()`, `est_prevision()`, `est_france()`, `est_export()`

### 3.3 OF (`of.py`)

```python
@dataclass
class OF:
    num_of: str                  # PK
    article: str                 # FK → Article
    description: str
    statut_num: int              # 1=Ferme, 2=Planifie, 3=Suggere
    statut_texte: str            # "Ferme", "Suggere"
    date_debut: date
    date_fin: date               # Planned end
    qte_a_fabriquer: int
    qte_fabriquee: int
    qte_restante: int
```

Methods: `is_ferme()`, `is_suggere()`

### 3.4 Nomenclature (`nomenclature.py`)

```python
@dataclass
class NomenclatureEntry:
    article_parent: str
    designation_parent: str
    niveau: int                  # 5, 10, 15, 20, 25...
    article_composant: str
    designation_composant: str
    qte_lien: float              # Per-unit quantity (can be decimal)
    type_article: str            # "Acheté" | "Fabriqué"
    nature_consommation: NatureConsommation  # FORFAIT | PROPORTIONNEL

@dataclass
class Nomenclature:
    article: str
    designation: str
    composants: list[NomenclatureEntry]
```

### 3.5 Other Models

| Model | Fields | Purpose |
|-------|--------|---------|
| `Stock` | article, stock_physique, stock_alloue, stock_bloque | `disponible() = physique - alloue` |
| `Gamme` | article, operations: list[GammeOperation] | Production routing |
| `GammeOperation` | article, poste_charge, libelle_poste, cadence | Single routing step |
| `Reception` | num_commande, article, code_fournisseur, quantite_restante, date_reception_prevue | Supplier delivery |
| `ChargeByPoste` | poste_charge, libelle_poste, charges: dict[week, hours] | Heatmap data point |
| `OFAllocation` | article, qte_allouee, num_doc, date_besoin | Allocation traceability |

---

## 4. Data Loading (`src/loaders/`)

### CSVLoader (`csv_loader.py`)

Low-level CSV reader. Supports two modes:
- **Standard**: Loads from `data/statique/` and `data/dynamique/`
- **Downloads**: Resolves timestamped files from `~/Downloads/`

Handles encoding (utf-8, latin-1), delimiters (`;`, `,`), and legacy format conversion (FLAG_CONTREMARQUE → TYPE_COMMANDE).

### DataLoader (`data_loader.py`)

High-level query interface wrapping CSVLoader. Key features:
- Lazy loading with property accessors
- Pre-built indexes: OFs by number, receptions by article, allocations by document
- Query methods: `get_ofs_by_article()`, `get_commandes_s1()`, `get_articles_fabrication()`
- Country-aware S+1 filtering (France = no forecasts, Export = optional)

---

## 5. Algorithms (`src/algorithms/`)

### 5.1 Charge Calculator (`charge_calculator.py`)

Core recursive algorithm for computing production hours per workstation.

**`calculate_article_charge(article, quantity, loader, visited=None) → dict[str, float]`**

1. Read the article's gamme (routing) → `hours = quantity / cadence` per poste
2. For each fabricated component in nomenclature → recurse with `qty = qte_lien * quantity`
3. Purchased components terminate recursion
4. Merge results by summing hours per poste

**`calculate_weekly_charge_heatmap(besoins, loader, num_weeks) → list[ChargeByPoste]`**

Main entry point for charge visualization:
- Separates besoins into BACKLOG, EN_COURS, S+1..S+N
- Consumes forecasts before computing charge
- Returns structured heatmap data

### 5.2 Forecast Consumption (`forecast_consumption.py`)

**`consume_forecasts_by_article(besoins) → (adjusted_besoins, stats)`**

1. Separate COMMANDE and PREVISION
2. Aggregate quantities per article
3. For each article: `net_forecast = max(0, total_forecast - total_command)`
4. Return commands unchanged + synthetic besoins for net forecasts only

### 5.3 Matching (`matching.py`)

**`CommandeOFMatcher`** — Matches customer orders to production orders.

**MTS path** (`_match_mts`): Direct lookup via `OF_CONTREMARQUE` field.

**NOR/MTO path** (`_match_nor_mto`):
1. Allocate available stock first
2. If article is ACHAT → no OF needed
3. Search for best OF: Ferme > Planifie > Suggéré
4. Sort by: status priority, date proximity, quantity available
5. Support OF sharing between multiple orders via `OFConso`

**Priority sorting**: `(command_type, expedition_date, order_age)`

### 5.4 Allocation Manager (`allocation.py`)

**`AllocationManager`** — Orchestrates virtual stock allocation with concurrency management.

**`StockState`** — Virtual stock tracker (`initial_stock`, `allocated_stock`). Methods: `get_available()`, `allocate()`, `add_supply()`.

**`AllocationStatus`** (enum): FEASIBLE, NOT_FEASIBLE, SKIPPED, DEFERRED

**Algorithm**:
1. Separate FERMES with existing ERP allocations from those needing virtual allocation
2. Optional pre-allocation evaluation via `AgentEngine` (may ACCEPT_PARTIAL)
3. Sort OFs by priority (date + feasibility)
4. Process each OF: create temporary `RecursiveChecker` with shared `StockState`, check feasibility, commit virtual allocations if feasible
5. Optional post-allocation evaluation for non-feasible OFs

---

## 6. Checkers (`src/checkers/`)

### Hierarchy

```
BaseChecker (ABC)
  └── RecursiveChecker
        ├── ImmediateChecker  (use_receptions=False)
        └── ProjectedChecker  (use_receptions=True)
```

### FeasibilityResult (`base.py`)

```python
@dataclass
class FeasibilityResult:
    feasible: bool = True
    missing_components: dict[str, int]
    alerts: list[str]
    depth: int = 0
    components_checked: int = 0
```

Methods: `add_missing()`, `add_alert()`, `merge(other)` — propagates infeasibility, accumulates missing/alerts.

### RecursiveChecker (`recursive.py`)

The most complex checker — **552 lines** of recursive BOM traversal.

**Core algorithm (`_check_article_recursive`)**:
1. Fetch nomenclature. If missing → alert (not blocking). If empty → feasible.
2. If parent OF is FERME → load existing ERP allocations
3. For each component:
   - **Purchased**: Check stock (virtual or real). Handle phantom articles (AFANT) → resolve to real variants.
   - **Fabricated**: Check stock first. If insufficient → search for a producing OF (ferme > planifie > suggéré) and recurse into it.
4. If no OF found for fabricated component → mark missing but still recurse BOM to identify root purchase shortages.

**Special features**:
- Phantom article resolution (`_check_phantom_component`): tries each variant until one covers 100%
- Subcontracted article handling (category starting "ST" → treat as purchase)
- Phantom sibling variant exclusion (prevents double-counting)

---

## 7. Scheduler (`src/scheduler/`)

### 7.1 Architecture

The scheduler has **two pipelines**:

| Pipeline | Entry Point | Status |
|----------|------------|--------|
| **Active** | `engine.run_schedule()` (exposed via `__init__.py`) | Functional |
| **Legacy** | `run.run_schedule()` | Broken (missing model classes) |

Only the active pipeline is described below.

### 7.2 Active Pipeline Flow

```
engine.run_schedule(loader, reference_date, ...)
│
├─ 1. Load weights from config/weights.json
├─ 2. Build working day horizon (5 days by default)
├─ 3. Identify target lines (PP_830, PP_153) and their articles
│
├─ 4. Select candidates (_select_candidates_from_matching)
│     ├─ Filter besoins by scope, type, horizon
│     ├─ Run CommandeOFMatcher
│     └─ Build CandidateOF objects + BDH buffer OFs
│
├─ 5. Dynamic capacity calculation
│     ├─ Per-line: target_hours / active_days * 1.10
│     └─ Cap at 14h/day
│
├─ 6. Pre-assign target days
│     ├─ Historical profile → assign to peak weekday
│     └─ Otherwise → round-robin across workdays
│
├─ 7. Day-by-day scheduling loop
│     for each workday:
│       apply receptions for the day
│       apply incoming buffer additions
│       for each line:
│         GenericLineScheduler.schedule_day()
│
├─ 8. KPI computation
│     ├─ taux_service: % of orders served on time or from stock
│     ├─ taux_ouverture: planned_hours / effective capacity
│     ├─ deviation_penalty: ratio of jumped-ahead OFs
│     ├─ jit_penalty: penalty for exact-due-date scheduling
│     └─ score = w1*service + w2*ouverture - w3*deviation - w4*jit
│
└─ 9. Output via reporting.write_outputs()
      ├─ planning_{line}.csv per line
      ├─ stock_BDH_projete.csv
      ├─ ofs_non_faisables.csv
      ├─ lignes_commande_statut.csv
      ├─ kpis.json
      └─ alertes.txt
```

### 7.3 Day-Level Scheduling (`lines.py` — `GenericLineScheduler.schedule_day()`)

**Algorithm**:
1. Filter unscheduled candidates with positive charge
2. **JIT deferral** (non-last-day): Split into "active" (due ≤1 day) and "deferred". Pull deferred only if active hours < max(7h, 50% capacity)
3. **Greedy loop**:
   - Sort remaining by `generic_sort_key` (12-tuple)
   - Find first candidate that fits capacity (+2.5h tolerance)
   - Check material availability via `availability_status` (tries J-2, J-1, J)
   - Check BDH buffer availability
   - Assign time slot (with 15min setup if article changes)
   - Update buffer, kanban, family counts
   - Reserve components in material state
4. **Under-capacity gate**: If planned < min_open_hours → cancel all assignments

### 7.4 Priority Heuristics (`heuristics.py`)

12-tuple sort key (lower = higher priority):

| Pos | Factor | Logic |
|-----|--------|-------|
| 0 | `priority` | 0=BDH shortage, 1=normal, 2=BDH no shortage |
| 1 | `due_urgency` | 0=overdue, 1=tomorrow, 2=in 2 days, 3=not urgent |
| 2 | `jit_bonus` | -2 if due exactly today |
| 3 | `prematurity` | Days until due (penalizes early scheduling) |
| 4 | `target_day_delta` | Distance from assigned target day |
| 5 | `due_date` | Exact due date |
| 6 | `-charge_hours` | Larger OFs first |
| 7 | `serie_bonus` | -2=same article, -0.5=shared BOM, 1=different |
| 8 | `mix_penalty` | 1 if family overrepresented |
| 9 | `kanban_penalty` | Cumulative consumption / 50 |
| 10 | `article` | Tiebreaker 1 |
| 11 | `num_of` | Tiebreaker 2 |

### 7.5 Material State (`material.py`)

Virtual stock management for scheduling:
- **`build_material_stock_state(loader) → StockState`**: Initialize from real stock
- **`apply_receptions_for_day()`**: Add supplier deliveries for a given day
- **`reserve_candidate_components()`**: Virtually reserve scarce purchase components (stock < 2x requirement)
- **`availability_status(checker, loader, candidate, day) → ("comfortable"|"tight"|"blocked", reason)`**: Three-tier check at J-2, J-1, J
- **`tracked_bdh_requirements()`**: Recursive BOM walk computing BDH component needs

### 7.6 Key Constants

| Constant | Value | Purpose |
|----------|-------|---------|
| `TARGET_LINES` | `("PP_830", "PP_153")` | Production lines |
| `TRACKED_BDH` | `("BDH2216AL", "BDH2231AL", "BDH2251AL")` | Buffer components |
| `BUFFER_THRESHOLDS` | `{673, 598, 598}` | Min stock per BDH |
| `MAX_DAY_HOURS` | `14.0` | Physical daily max |
| `SETUP_TIME_HOURS` | `0.25` | 15 min changeover |
| `PLANNING_WORKDAYS` | `5` | Default horizon |
| `DEFAULT_WEIGHTS` | `{w1:0.7, w2:0.2, w3:0.1}` | Scoring weights (w4=0.1 used in engine but not in config) |

### 7.7 Dead Code

The following files form a **legacy pipeline** that is non-functional (missing model classes):
- `run.py` — Old orchestrator
- `sequencer.py` — Old greedy sequencer
- `kpi.py` — Old KPI module (also duplicates `weights.py`)
- `buffer_manager.py` — Old BDH buffer manager
- `demand_solver.py` — Old candidate builder

---

## 8. Agents (`src/agents/`)

### 8.1 Architecture

The agents module is a **dual-mode decision intelligence system**:

```
AgentEngine (facade)
├── Static Mode: SmartDecisionRule
│     ├── CompletionCriterion (weight: 0.5)
│     ├── ClientCriterion (weight: 0.3)
│     └── UrgencyCriterion (weight: 0.2)
│
└── LLM Mode: LLMDecisionAgent
      ├── LLMContextBuilder → builds structured analysis
      ├── LLMPromptBuilder → constructs markdown prompt
      ├── BaseLLMClient → MistralLLMClient or MockLLMClient
      └── LLMResponseParser → extracts + validates JSON
```

### 8.2 Decision Types

```python
class AgentAction(Enum):
    ACCEPT_AS_IS      # Fully acceptable
    ACCEPT_PARTIAL    # Accept with reduced quantity
    REJECT            # Not feasible
    DEFER             # Defer to later date
    DEFER_PARTIAL     # Partial + defer remainder
```

### 8.3 Static Rule Engine (`smart_rule.py`)

**Decision hierarchy**:
1. If any criterion explicitly suggests an action → use it (ACCEPT_AS_IS > ACCEPT_PARTIAL > REJECT)
2. Otherwise → use weighted score thresholds:
   - score ≥ 0.7 → ACCEPT_AS_IS
   - score ≤ 0.3 → REJECT
   - Otherwise → ACCEPT_PARTIAL

### 8.4 LLM Pipeline

**Three-tier resilience**:
1. **Prefilter** — resolve trivial cases without LLM call (feasible → ACCEPT, non-feasible without blocked/reception → REJECT)
2. **LLM call** — with exponential backoff retry
3. **Contextual fallback** — on error, always REJECT

**Prompt structure**: Markdown table of components (article, level, type, required, stock, situation, ratio, receptions), critical component cards, situation analysis, strict JSON response template.

### 8.5 Agent Tools (`tools/`)

| Tool | File | Purpose |
|------|------|---------|
| Rescheduling Messages | `rescheduling_messages.py` | Generates RETARD, RETARD_IMMINENT, URGENCE, DEBLOCAGE messages |
| Late Receptions | `late_receptions.py` | Impact analysis of delayed supplier deliveries |
| Schedule Simulator | `schedule_simulator.py` | What-if charge simulation |
| OF Sequencer | `of_sequencer.py` | Sequences OFs on a post (EDD, FIFO, SPT rules) |
| Component Competition | `component_competition.py` | OF competition analysis for a component |
| Service Rate KPIs | `service_rate_kpis.py` | Global and per-client service rates |
| OF Affirm Suggester | `of_affirm_suggester.py` | Suggests which OFs to firm up |
| Week Summary | `week_summary.py` | Daily scheduler briefing (aggregates 4 tools) |
| Bottleneck Detector | `bottleneck_detector.py` | Identifies production bottlenecks |

### 8.6 Organization Agent (`organization/`)

Workshop organization analysis pipeline:
1. **ChargeCalculator** — Recursive charge by poste, S+1 through S+4
2. **TrendAnalyzer** — Linear regression on weekly charge (UPWARD/STABLE/DOWNWARD)
3. **OrganizationEvaluator** — Selects optimal organization (1×8=35h, 2×8=70h, 3×8=105h)

### 8.7 Scheduling Agent (`scheduling/`)

Weekly charge planning for gap-filling:
1. Extract stockout components from S+1 feasibility results
2. Build S+1 poste schedule from feasible OFs
3. Identify under-loaded postes (gaps)
4. Find S+2/S+3 candidates
5. Score by composite: 50% urgency + 30% component overlap (Jaccard) + 20% feasibility
6. Greedy fill until minimum charge reached

---

## 9. Reports (`src/reports/`)

### 9.1 Action Report (`action_report.py` — 1449 lines)

The most comprehensive report. Produces **4 complementary views**:

1. **Component Actions** — One line per blocking component (missing qty, impacted OFs/commands, urgency classification)
2. **Supplier Actions** — One line per supplier/purchase-order (delayed receptions)
3. **Poste de Charge Risk** — Workstations at risk of shutdown
4. **Kanban Risk** — Feeder workstations below coverage threshold

**Urgency classification**: RETARD_FOURNISSEUR > AUCUNE_COUVERTURE > COUVERTURE_TARDIVE > SURVEILLANCE

**Kanban analysis**: Computes stock coverage in days, compares to threshold based on BOM depth and handoff delays.

### 9.2 S+1 Report (`rapport_s1.py`)

Rich table display: Command, Client, Article, Qty, Expedition, Besoin net, Couverture, OF matché, OK?, Manquants.

---

## 10. Entry Points

| Entry Point | Interface | Target User |
|-------------|-----------|-------------|
| `main.py` (root) → `src/main.py` | CLI (argparse) | Developers / automation |
| `menu.py` | Interactive REPL (Rich + questionary) | Production planners |
| `src/api/server.py` | REST API (FastAPI) | Local GUI frontend |
| `analyze_charge.py` | Specialized CLI | Charge analysts |

### CLI Modes (`src/main.py`)

| Flag | Mode | Pipeline |
|------|------|----------|
| `--schedule` | Scheduler | `run_schedule()` → KPIs + heatmap |
| `--charge-heatmap` | Heatmap | `calculate_weekly_charge_heatmap()` |
| `--s1` | S+1 validation | `main_s1()` → matching + feasibility + reports |
| `--organization` | Workshop org | `OrganizationAgent.analyze()` |
| `--commande NUM` | Single order | `RecursiveChecker.check_commande()` |
| `--of NUM` | Single OF | All three checkers |
| (default) | Full verification | Immediate + Projected + Allocation |

---

## 11. API (`src/api/server.py`)

FastAPI server with CORS open for local GUI usage.

| Method | Path | Description |
|--------|------|-------------|
| GET | `/health` | Health check |
| GET | `/config` | GUI configuration |
| POST | `/data/load` | Load CSV data |
| POST | `/runs/s1` | Launch S+1 analysis (async) |
| GET | `/runs/{run_id}` | Poll S+1 status |
| GET | `/reports/actions/latest` | Latest action report |
| GET | `/reports/s1/latest` | Latest S+1 report |
| GET | `/reports/files` | List all reports |

S+1 runs are asynchronous (background thread, UUID-based polling).

---

## 12. Dependency Map

```
Entry Points
  └── DataLoader (loaders/)
        ├── CSVLoader → CSV files
        └── Models (models/)
              ├── Article, BesoinClient, OF, Stock
              ├── Nomenclature, Gamme
              └── Reception, ChargeByPoste, Allocation

Core Pipeline
  DataLoader → CommandeOFMatcher (algorithms/matching)
             → Checkers (checkers/)
                  └── RecursiveChecker → Nomenclature (recursive BOM)
             → AllocationManager (algorithms/allocation)
                  └── StockState (virtual stock)

Scheduler Pipeline
  DataLoader → engine.run_schedule()
             → GenericLineScheduler (scheduler/lines)
                  → heuristics.generic_sort_key()
                  → material.* (virtual stock, feasibility)
                  → RecursiveChecker (component checking)
             → reporting.write_outputs()

Decision Pipeline
  AgentEngine (agents/engine)
    ├── SmartDecisionRule → Criteria (completion, client, urgency)
    └── LLMDecisionAgent → ContextBuilder → PromptBuilder → Mistral → ResponseParser

Reports Pipeline
  Matching Results + Feasibility Results
    → build_action_report() → 4 views (components, suppliers, postes, kanban)
    → format_rapport_s1() → Rich table
```

---

## 13. Configuration

### Scheduler Weights (`config/weights.json`)

```json
{
  "w1": 0.7,   // Service rate weight
  "w2": 0.2,   // Opening rate weight
  "w3": 0.1    // Deviation penalty weight
}
```

Auto-created with defaults if missing. Normalized to sum=1.0.

### Decision Config (`config/decisions.yaml`)

```yaml
smart_rule:
  criteria_weights:
    completion: 0.5
    client: 0.3
    urgency: 0.2
  thresholds:
    accept: 0.7
    reject: 0.3
  urgency:
    very_urgent: 3   # days
    urgent: 7
    comfortable: 21
```

### Project Setup (`pyproject.toml`)

- Python ≥ 3.11
- Build: hatchling
- Core: pandas, rich, python-dateutil, fastapi, uvicorn
- Dev: pytest, pytest-cov, black, ruff
- Line length: 100

---

## 14. Technical Debt

| Area | Issue | Impact |
|------|-------|--------|
| **Dead code** | `scheduler/run.py`, `sequencer.py`, `kpi.py`, `buffer_manager.py`, `demand_solver.py` — non-functional, missing model classes | Confusion, import errors |
| **Duplicated constants** | BUFFER_THRESHOLDS in `material.py` + `buffer_manager.py`; weights in `kpi.py` + `weights.py`; SETUP_TIME in `engine.py` + `lines.py` | Drift risk |
| **Mutable state** | OF `qte_restante` mutated in-place during allocation then restored | Exception-safety risk |
| **Large files** | `action_report.py` (1449 lines) mixes aggregation and presentation | Maintainability |
| **Private coupling** | `AllocationManager` accesses `data_loader._receptions_by_article` (private attribute) | Fragile |
| **Patch scripts** | `patch_engine.py`, `patch_smooth.py` use exact string matching — inherently one-use | Not part of runtime but exist in repo |

---

## 15. Test Coverage

```
tests/
├── conftest.py
├── test_*.py                    # Core unit tests (19 files)
├── agents/
│   ├── organization/            # 8 test files
│   ├── scheduling/              # 6 test files
│   ├── test_criteria/           # 4 test files
│   └── tools/                   # 9 test files
```

Key test areas: matching, allocation, recursive checker, scheduler KPIs, capacity models, charge calculation, forecast consumption, GUI API.
