## Issue #52 — Fin du découpage SOLID : `of-detail-sheet.tsx` + `shortage-table.tsx`

### Contexte

Les 3 pages sont déjà découpées (commits `1c1fbdd`, `bfe2d6b`, `6382246`). Il reste **2 composants** de la liste de l'issue. Le découpage suit le pattern établi : _1 vue/mode = 1 fichier composant_, _dérivations pures → `inertia/lib/<feature>/`_, le fichier d'origine devient un shell/orchestrateur. **Pixel-perfect requis** (JSX déplacé tel quel, zéro retouche visuelle) — contrainte identique aux 3 précédents.

Le projet n'a pas de tests frontend et les 3 refactos précédents n'en ont pas ajouté : validation par `npm run typecheck` uniquement (règle CLAUDE.md).

---

### Fichier 1 — `inertia/components/of/of-detail-sheet.tsx` (686 → ~190 l.)

3 sections distinctes dans le composant, chacune extraite :

**Dérivations pures → `inertia/lib/of/` (testables)**

- `diagnostic-types.ts` — types `DiagNode`/`DiagShort`/`DiagCovering`/`DiagResult` (miroir de `RecursiveDiagnosticResult` serveur), maps `STATUS_LABEL`/`STATUS_VARIANT`/`TREE_STATUS_LABEL`/`STATUT_OF`, helper `fmtDateFr`. Sortis du fichier de composant vers une dépendance pure (uniquement le type `OfDetail` restant dans `lib/of/types.ts`).
- _(Les helpers `statusVariant`, `isSuggestion`, `rupturedComponents`, `canFirm` restent dans le composant — lisent les signaux/accessors, ne sont pas des fonctions pures.)_

**Composants de vue → `inertia/components/of/`**

- `of-diagnostic-tree.tsx` (~120 l.) — l'arbre diagnostic récursif : `DiagColHeader`, `DiagRow`, `DiagShortRow` (la récursion). Exporte un composant `<OfDiagnosticTree result={dr()} />`.
- `of-firm-action.tsx` (~90 l.) — le bouton Affermir + son popover de confirmation rupture (backdrop + liste composants + bouton destructif). Reçoit l'état via props : `{ detail, firming, confirmRupture, rupturedComponents, onFirm, onDoFirm, onCancelConfirm, firmMsg }`.
- _(le tab "Composants" — récap ruptures + table BOM — reste dans le shell : ~70 l. inline, pas une récursion complexe, et lié intimement à `d()`.)_

**Shell** (`of-detail-sheet.tsx`) — garde : la déclaration `<Sheet>`, le fetch detail + diag (resources), l'état (tab/firming/firmMsg/confirmRupture), la fonction `doFirm`, la barre d'identité, méta+avancement, les onglets, et les 2 contenus tab (composants inline + diagnostic via `<OfDiagnosticTree>`). `Meta` et `TabBtn` restent co- localisés (petits, privés au shell).

> Pas de changement d'API publique : `export default OfDetailSheet` inchangé, même signature de props. Les 3 consommateurs (`scheduling.tsx`, `shortages.tsx`, `programme.tsx`) ne bougent pas.

---

### Fichier 2 — `inertia/components/shortages/shortage-table.tsx` (645 → ~25 l.)

Le fichier porte déjà 3 composants exportés distincts (`ShortageRegistre` R1, `ShortageComposants` R2, `ShortageTimeline` R3) + 1 helper visuel (`Marker`). Découpage 1-vue = 1-fichier :

**Dérivations pures → `inertia/lib/shortages/` (testables)**

- `shortage-math.ts` — `groupByComponent` + `ComponentGroup`/`VERDICT_RANK`/`VERDICT_BADGE` (l'agrégation « dégâts par composant » de R2, pure) ; `offsetPct` (position temporelle de la frise R3, pure) ; `isLate`/`isAtRisk` (prédicats verdict, purs). Les constantes `TH`/`TH_R`/`TD` (classes de cellules Papier partagées par R1+R2) y vont aussi.

**Vues → `inertia/components/shortages/` (1 fichier par vue)**

- `shortage-registre.tsx` (~210 l.) — `ShortageRegistre` (R1, DataTable colonnes TanStack).
- `shortage-composants.tsx` (~150 l.) — `ShortageComposants` (R2, table agrégée).
- `shortage-timeline.tsx` (~180 l.) — `ShortageTimeline` (R3, frise) + `Marker` (helper privé à la frise).

**Barrel** (`shortage-table.tsx`, ~25 l.) — re-exports nommés pour ne **pas casser l'import** du parent :

```ts
export { ShortageRegistre } from './shortage-registre'
export { ShortageComposants } from './shortage-composants'
export { ShortageTimeline } from './shortage-timeline'
```

`shortages.tsx` (`import { ShortageComposants, ShortageRegistre, ShortageTimeline } from '@/components/shortages/shortage-table'`) reste inchangé. _(Alternative : modifier l'import du parent vers les 3 fichiers — mais le barrel préserve la compat et c'est le choix le moins risqué pour un refacto "aucune retouche".)_

---

### Validation

- `npm run typecheck` (gate obligatoire, CLAUDE.md) — check que tous les imports se résolvent.
- Vérification : `wc -l` sur les nouveaux fichiers pour confirmer la réduction.
- Pas de test ajouté (convention du projet : aucun test frontend, les 3 refactos précédents n'en ont pas).
- Pas de Playwright (interdit par CLAUDE.md).

### Commit

Un seul commit, message au format des 3 précédents :

```
refacto(SOLID): of-detail-sheet + shortage-table → composants/lib dédiés (issue #52)
```

Puis `git push` (workflow obligatoire CLAUDE.md).

### Hors-scope (signalé, pas fait)

L'issue note que `shortage-table` pourrait être repris « d'office » par la refonte /ruptures si elle ajoute des vues — mais la refonte n'est pas en cours (pas de branche/trace), donc le découpage mécanique maintenant clotûre proprement l'issue.
