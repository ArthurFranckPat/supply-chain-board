# Archive — mockups HTML

Les maquettes HTML standalone ne sont **pas** sur `master` (production, socle
seulement). Elles restent sur `dev` : `_design/archive/`.

Le design system vivant est dans le code :

- tokens et thèmes : `resources/css/app.css`
- composants : `inertia-react/components/`
